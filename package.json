{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "start:api": "json-server --watch db.json --port 5001"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3",
    "typescript": "~5.9.3",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "prettier": "^3.6.2"
  }
}
